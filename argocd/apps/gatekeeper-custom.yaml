# ArgoCD App: Deploy custom Gatekeeper ConstraintTemplate + Constraint
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatekeeper-custom
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://github.com/LongITer/w-10.git
    path: gatekeeper/constraints
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: gatekeeper-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - ServerSideApply=true
