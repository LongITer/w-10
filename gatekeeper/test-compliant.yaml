# Test Compliant: Pass all 4 Gatekeeper constraints
# Expected: ACCEPTED by all constraints
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-compliant
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-compliant
  template:
    metadata:
      labels:
        app: test-compliant
    spec:
      containers:
      - name: app
        image: nginx:1.21  # ✓ Specific version tag (not :latest)
        resources:
          limits:  # ✓ Has resource limits
            cpu: 100m
            memory: 64Mi
          requests:
            cpu: 50m
            memory: 32Mi
        securityContext:
          runAsUser: 1000  # ✓ Non-root user
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
